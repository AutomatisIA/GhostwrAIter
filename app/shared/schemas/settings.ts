// Both settings IPC channels (`settings:export-workspace` and
// `settings:purge-execution-logs`) take no input. They use the shared
// `emptyInputSchema` from `common.ts`.
export { emptyInputSchema } from "./common";
